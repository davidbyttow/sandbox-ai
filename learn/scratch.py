import numpy as np

# Create a 3 dimensional vector initialized to 0, 1, 2
v = np.arange(3)

# Use dot product on v with itself
print(v.dot(v))

# Create a 2x2 matrix initialized to 0, 1, 2, 3
m = np.arange(4).reshape((2, 2))
